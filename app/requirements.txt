flask==3.0.3
flask-login==0.6.3
flask-wtf==1.3.0
gunicorn==22.0.0
requests==2.32.3
anthropic>=0.40.0
SQLAlchemy==2.0.32
psycopg2-binary==2.9.9
werkzeug==3.0.4
APScheduler==3.10.4
reportlab==4.2.2
openpyxl==3.1.5
cryptography==43.0.1
# 2026-09, Item J auditi (🟡 O'RTA, 20-band -- markazlashgan vaqt zonasi,
# tz_utils.py): `python:3.11-slim` (Dockerfile) OS darajasida IANA tzdata
# bazasini o'z ichiga olmasligi mumkin -- `zoneinfo` shu holda avtomatik
# ushbu sof-Python paketga zaxira sifatida murojaat qiladi (CPython
# hujjatida rasman tavsiya etilgan naqsh).
tzdata==2024.2
# 2026-09, Item J auditi (🟡 O'RTA, 19-band -- migratsiya tizimi yo'q edi):
# versiyalangan sxema migratsiyalari uchun (qarang app/migrations/).
# `db._migrate_add_missing_columns()` ham xavfsizlik zaxirasi sifatida
# saqlanib qolmoqda -- Alembic ATAYLAB avtomatik deploy oqimiga
# ulanmagan (production'da bir martalik `alembic stamp head` kerak,
# app/migrations/versions/..._boshlangich_sxema_...py fayliga qarang).
alembic==1.13.2
